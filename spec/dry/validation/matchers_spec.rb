require "spec_helper"

describe Dry::Validation::Matchers do
  it "has a version number" do
    expect(Dry::Validation::Matchers::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
